# ============================================
# Mantyl Sequence Generator — Environment Variables
# ============================================
# Copy this file to .env and fill in your keys.
# NEVER commit .env to version control.
# In production, set these in Netlify → Site Settings → Environment Variables.
# ============================================

# Apollo API — Prospect search (REQUIRED)
# Get your key at: https://app.apollo.io → Settings → Integrations → API Keys
# Use the standard API key (not OAuth)
APOLLO_API_KEY=

# Anthropic Claude API — Personalized copy generation (REQUIRED)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Clay Webhook — Optional prospect enrichment (verified emails/phones)
# Set up a Clay webhook table, then paste the webhook URL here.
# Leave blank to skip Clay enrichment and use Apollo data only.
CLAY_WEBHOOK_URL=
CLAY_WEBHOOK_AUTH=
